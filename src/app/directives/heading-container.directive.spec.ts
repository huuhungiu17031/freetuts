import { HeadingContainerDirective } from './heading-container.directive';

describe('HeadingContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingContainerDirective();
    expect(directive).toBeTruthy();
  });
});
